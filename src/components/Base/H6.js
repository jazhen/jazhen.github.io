import styled from "styled-components";

const H6 = styled.h6`
  font-family: "Poppins", "Helvetica", "Arial", sans-serif;
  font-size: clamp(1rem, 0.9063rem + 0.4688vw, 1.1875rem);
  font-weight: 500;
  letter-spacing: 0.15px;
  margin: 0;
`;

export default H6;
