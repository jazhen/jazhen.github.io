import styled from "styled-components";

const H5 = styled.h5`
  font-family: "Poppins", "Helvetica", "Arial", sans-serif;
  font-size: clamp(1.1875rem, 1.0625rem + 0.625vw, 1.4375rem);
  font-weight: 400;
  letter-spacing: 0px;
  margin: 0;
`;

export default H5;
