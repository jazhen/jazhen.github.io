import styled from "styled-components";

const H2 = styled.h2`
  font-family: "Poppins", "Helvetica", "Arial", sans-serif;
  font-size: clamp(2.9375rem, 2.5938rem + 1.7188vw, 3.625rem);
  font-weight: 400;
  letter-spacing: -0.5px;
  margin: 0;
`;

export default H2;
